{
    view = {
        el:".playlists"
    }
    model = {}
    controller = {
        init(view,model){
            this.view = view;
            this.model = model;
            this.bindtap();

        },
        bindtap(){
        }
    }
    controller.init(view,model)
}